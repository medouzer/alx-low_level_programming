#include "main.h"

/**
 * _abs - the absolute value of an integer
 * Return: return the abs value of number
 * @n: is the number for test
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
